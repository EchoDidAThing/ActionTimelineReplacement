using ActionTimelineReplacement.Sheets;
using Dalamud.Game.Text.SeStringHandling;
using Dalamud.Game.Text.SeStringHandling.Payloads;
using Dalamud.Utility;
using FFXIVClientStructs.FFXIV.Component.GUI;
using System;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using System.Diagnostics.CodeAnalysis;
using System.Runtime.CompilerServices;
using System.Text;
using System.Text.RegularExpressions;

namespace ActionTimelineReplacement.Base.Global;
internal class DetourGlobals
{
    //private void ReplaceSeString(ref SeString seString, string baseString, string replaceString)
    //{
        /*if (petData == null)
        {
            return;
        }

        if (!colourConfig.Enabled)
        {
            return;
        }

        user ??= UserList.LocalPlayer;

        if (user == null)
        {
            return;
        }

        string? baseName = PetServices.NameService.GetName(nameType, petData);

        if (baseName.IsNullOrWhitespace())
        {
            return;
        }

        string? customName = user.GetCustomName(petData);

        if (customName.IsNullOrWhitespace())
        {
            return;
        }*/

        //ReplaceSeString(ref seString, baseName, customName, edgeColour, textColour);
        //ReplaceSeString(ref seString, baseName, customName);
    //}
}
